'use client';

// Custom cursor component - currently disabled to avoid browser compatibility issues
// Can be re-enabled once fully tested across all browsers

export function CustomCursor() {
  // Return null to disable custom cursor for now
  // All other animations and interactions are still active
  return null;
}
